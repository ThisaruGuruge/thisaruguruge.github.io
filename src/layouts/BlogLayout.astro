---
import Layout from './Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';

interface Props {
  title: string;
  description?: string;
  pubDate: Date;
  author?: string;
}

const { title, description, pubDate, author = 'Thisaru' } = Astro.props;
---

<Layout title={`${title} | Thisaru's Blog`} description={description}>
  <Header />
  <main>
    <article class="py-20">
      <div class="container max-w-3xl">
        <header class="mb-12">
          <h1 class="heading-1 mb-4">{title}</h1>
          <div class="flex items-center text-gray-400 space-x-4">
            <span>{author}</span>
            <span>•</span>
            <time datetime={pubDate.toISOString()}>
              {pubDate.toLocaleDateString('en-US', {
                year: 'numeric',
                month: 'long',
                day: 'numeric',
              })}
            </time>
          </div>
        </header>
        <div class="prose prose-invert">
          <slot />
        </div>
      </div>
    </article>
  </main>
  <Footer />
</Layout>
